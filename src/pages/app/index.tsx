import { Layout, LayoutBody, LayoutHeader } from '@/layout';

export const PageHome = () => {
    return (
        <Layout>
            <LayoutHeader />
            <LayoutBody />
        </Layout>
    );
};

export default PageHome;
